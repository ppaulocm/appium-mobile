import { When, Then } from 'cucumber';

When('player insert a new nickname', (text) => {

})

When('tap on validate button', (text) => {

})

When('player tap on Save', (text) => {

})

Then('dialog box open to edit user nickname', (text) => {

})

Then('message indicating nickname availability is displayed', (text) => {

})








